## SENTINEL incident `INC-2026-003` — crashloop

**Root cause:** OOM confirmed in container logs: FATAL: OOM allocating 512MiB

**Evidence**
- OOM confirmed in container logs: FATAL: OOM allocating 512MiB

**Proposed fix:** Increase api-server memory limit to 896Mi (observed peak ~512Mi)

**Risk score:** 3/10  ·  **Confidence:** MEDIUM

**fix_plan**
```json
{
  "kind": "memory_limit",
  "verb": "patch",
  "resource": "deployment",
  "patch": {
    "spec": {
      "template": {
        "spec": {
          "containers": [
            {
              "name": "api-server",
              "resources": {
                "limits": {
                  "memory": "896Mi"
                },
                "requests": {
                  "memory": "448Mi"
                }
              }
            }
          ]
        }
      }
    }
  },
  "summary": "Increase api-server memory limit to 896Mi (observed peak ~512Mi)"
}
```

**Live apply result:** `OK` — `kubectl patch deployment api-server -n prod -p {"spec": {"template": {"spec": {"containers": [{"name": "api-server", "resources": {"limits": {"memory": "896Mi"}, "requests": {"memory": "448Mi"}}}]}}}}`

_Opened automatically after human approval. Review before merge._
