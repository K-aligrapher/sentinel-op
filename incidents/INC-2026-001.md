## SENTINEL incident `INC-2026-001` — crashloop

**Root cause:** OOMKilled — container exceeded its memory limit (exit 137)

**Evidence**
- OOMKilled — container exceeded its memory limit (exit 137)

**Proposed fix:** Increase api-server memory limit to 256Mi (OOMKilled at its 128Mi limit; no usage sample available)

**Risk score:** 3/10  ·  **Confidence:** MEDIUM

**fix_plan**
```json
{
  "kind": "memory_limit",
  "verb": "patch",
  "resource": "deployment",
  "patch": {
    "spec": {
      "template": {
        "spec": {
          "containers": [
            {
              "name": "api-server",
              "resources": {
                "limits": {
                  "memory": "256Mi"
                },
                "requests": {
                  "memory": "128Mi"
                }
              }
            }
          ]
        }
      }
    }
  },
  "summary": "Increase api-server memory limit to 256Mi (OOMKilled at its 128Mi limit; no usage sample available)"
}
```

**Live apply result:** `OK` — `kubectl patch deployment api-server -n prod -p {"spec": {"template": {"spec": {"containers": [{"name": "api-server", "resources": {"limits": {"memory": "256Mi"}, "requests": {"memory": "128Mi"}}}]}}}}`

_Opened automatically after human approval. Review before merge._
