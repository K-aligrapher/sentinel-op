[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
addopts = -ra
markers =
    integration: requires external infrastructure (Minikube, MCP servers, Daytona)
    e2e: full incident lifecycle, may need infrastructure
