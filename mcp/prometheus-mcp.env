# prometheus-mcp — run as:
#   PROMETHEUS_URL=http://host.docker.internal:9090 npx prometheus-mcp@latest http --port 8001
PROMETHEUS_URL=http://localhost:9090
PROMETHEUS_MCP_PORT=8001
PROMETHEUS_MCP_TRANSPORT=http
