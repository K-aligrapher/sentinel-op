# Pinned runtime + test dependencies for SENTINEL.
# Regenerate the fully-hashed lock file with:
#   pip-compile requirements.in --generate-hashes
structlog==23.3.0
python-dotenv==1.0.1
httpx==0.27.0
pyyaml==6.0.1
pytest==8.1.1
pytest-asyncio==0.23.6
kubernetes==29.0.0
prometheus-api-client==0.5.3
aiohttp==3.9.5
rich==13.7.1
